# AUV-Simulation

# Setup
I suggest setting up a Python virtual environment in Python 3.14 and installing the required packages to run this.
```
py -3.14 -m venv .venv
.venv\Scripts\activate
python -m pip install -r requirements.txt
```